##在这里注明自己的服务注册中心地址
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
server:
  port: 8763
spring:
  application:
    name: service-hi

    ##这里的spring.application.name在以后服务于服务之间相互调用一般都是根据这个